# Python tooling for the `ai/` half of the monorepo (ADR-0003, ADR-0005).
#
# Runtime dependencies for individual AI services live with those services, not here.
# This file is only what is needed to lint and format the Python tree:
#
#   pip install -r requirements-dev.txt
#   ruff check ai/          # or: pnpm lint:py
#   python -m pytest        # or: pnpm test:py
#
# Pinned so CI and local runs enforce the same rules — a different Ruff version means a
# different rule set, which means the boundary bans in ruff.toml stop being a guarantee.
#
# SUPERSEDED BUT NOT YET DELETED (ADR-0006). The uv workspace at `ai/pyproject.toml` now declares
# these same pins in its `dev` group, and `uv.lock` is committed. This file survives only because
# `.github/workflows/ci.yml` still runs `pip install -r requirements-dev.txt`; deleting it before
# that workflow switches to `uv sync --frozen` would break every CI run.
#
# **These versions are duplicated and must be changed in both places until then.**
ruff==0.16.0
pytest==9.1.1

# STOPGAP — these two are RUNTIME dependencies of ai/resume-parser (ADR-0016), not dev tooling, and
# they do not belong in this file. They are here only because CI installs this file with pip and
# never reads ai/uv.lock, so without them `python -m pytest` fails on `import docx` while every
# local run through uv passes. A green local suite and a red CI on the same commit is the worst of
# both.
#
# They are pinned to exactly what ai/uv.lock resolves, and must be changed together with
# ai/resume-parser/pyproject.toml.
#
# **All of this disappears in one change**: point .github/workflows/ci.yml at
# `uv sync --frozen --all-packages`, then delete this file. That needs a token with the `workflow`
# OAuth scope — `gh auth refresh -h github.com -s workflow`.
pypdf==6.1.3
python-docx==1.2.0
fastapi==0.121.2
pydantic==2.12.4
httpx==0.28.1
uvicorn==0.38.0
